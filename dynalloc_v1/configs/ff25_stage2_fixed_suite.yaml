experiment:
  name: ff25_stage2_fixed_suite
  output_dir: outputs/ff25_stage2_fixed_suite
  notes: stage-2 comparison only using the base model spec

universe:
  profile: ff25_szbm_with_bond

model:
  profile: base_estimated_pls3

constraints:
  profile: long_only_cash

method:
  profile: suite

split:
  profile: pre2005_final20y

selection:
  profile: disabled

comparison:
  profile: fixed_base_model

training:
  iters: 800
  batch_size: 512
  lr: 0.0003
  gamma: 5.0
  residual_policy: true
  residual_ridge: 1.0e-06
  ppgdpo_mc: 256
  ppgdpo_subbatch: 64
  ppgdpo_seed: 123

runtime:
  backend: legacy_bridge
  legacy_entrypoint: legacy/vendor/pgdpo_legacy_v69/run_french49_10y_model_based_latent_varx_fred.py
  legacy_workdir: legacy/vendor/pgdpo_legacy_v69
  seed: 0
  device: cuda
  dtype: float64
  fred_api_key_env: FRED_API_KEY
